{
    "id": "477574",
    "title": "Magic Circle",
    "url": "https://openprocessing.org/@u67512/477574",
    "code": "// === Tab: mySketch ===\n// Title: Magic Circle\n// Author: FAL\n// Date: 21. Nov. 2017\n// Made with p5.js v0.5.16\n// Generated by TypeScript v2.6.1\n\n\n/*\n  LICENSE:\n    \"greek-subset.ttf\" is a subset font derived from EB Garamond\n    (Copyright 2017 The EB Garamond Project Authors https://github.com/octaviopardo/EBGaramond12)\n    and licensed under the SIL Open Font License, Version 1.1 (http://scripts.sil.org/OFL).\n*/\n\"use strict\";\n//\n// ------------ Array loop utility -------------------------------------\n//\nfunction loopArray(array, callback) {\n    for (let i = array.length - 1; i >= 0; i -= 1) {\n        callback(array[i], i, array);\n    }\n}\n//\n// ------------ Array wrapper -----------------------------\n//\n/// <reference path=\"ArrayLoopUtility.ts\" />\nclass ArrayWrapper {\n    constructor() {\n        this.array = [];\n    }\n    get length() {\n        return this.array.length;\n    }\n    get(index) {\n        return this.array[index];\n    }\n    getLast() {\n        return this.array[this.array.length - 1];\n    }\n    push(element) {\n        this.array.push(element);\n    }\n    pop() {\n        return this.array.pop();\n    }\n    loop(callback) {\n        loopArray(this.array, callback);\n    }\n}\n//\n// ------------ Drawable -----------------------------\n//\n/// <reference path=\"ArrayWrapper.ts\" />\nclass DrawableArray extends ArrayWrapper {\n    static drawFunction(value) {\n        value.draw();\n    }\n    draw() {\n        this.loop(DrawableArray.drawFunction);\n    }\n}\n//\n// ------------ Background ---------------------------------------\n//\n/// <reference path=\"../../../my_types/p5/p5.global-mode.d.ts\" />\n/// <reference path=\"Drawable.ts\" />\nclass AlphaBackground {\n    constructor(backgroundColor, drawIntervalFrameCount = 1, blendModeString, defaultBlendModeString) {\n        this.backgroundColor = backgroundColor;\n        this.drawIntervalFrameCount = drawIntervalFrameCount;\n        this.blendModeString = blendModeString;\n        this.defaultBlendModeString = defaultBlendModeString;\n    }\n    draw() {\n        if (frameCount % this.drawIntervalFrameCount !== 0)\n            return;\n        if (this.blendModeString)\n            blendMode(this.blendModeString);\n        noStroke();\n        fill(this.backgroundColor);\n        rect(0, 0, width, height);\n        if (this.defaultBlendModeString)\n            blendMode(this.defaultBlendModeString);\n    }\n}\n//\n// ------------ Steppable -----------------------------\n//\n/// <reference path=\"ArrayWrapper.ts\" />\nclass SteppableArray extends ArrayWrapper {\n    static stepFunction(value) {\n        value.step();\n    }\n    step() {\n        this.loop(SteppableArray.stepFunction);\n    }\n}\n//\n// ------------ Angle quantity -----------------------------\n//\n/// <reference path=\"Steppable.ts\" />\nclass AngleQuantity {\n    constructor(angle = 0, angleVelocity = 0) {\n        this.angle = angle;\n        this.angleVelocity = angleVelocity;\n    }\n    step() {\n        this.angle += this.angleVelocity;\n    }\n}\n//\n// ------------ Sprite -------------------------------------\n//\n/// <reference path=\"ArrayWrapper.ts\" />\n/// <reference path=\"Steppable.ts\" />\n/// <reference path=\"Drawable.ts\" />\nclass SpriteArray extends ArrayWrapper {\n    constructor() {\n        super(...arguments);\n        this.draw = DrawableArray.prototype.draw;\n        this.step = SteppableArray.prototype.step;\n    }\n}\n//\n// ------------ Circular text ---------------------------------------\n//\n/// <reference path=\"../../../my_types/p5/p5.global-mode.d.ts\" />\nclass CircularText {\n    constructor(textString, circleSize) {\n        this.textString = textString;\n        this.circleSize = circleSize;\n    }\n    static draw(s, circleSize) {\n        const radius = 0.5 * circleSize;\n        for (let i = 0, len = s.length; i < len; i += 1) {\n            const angle = (i / len) * TWO_PI - HALF_PI;\n            const x = radius * Math.cos(angle);\n            const y = radius * Math.sin(angle);\n            translate(x, y);\n            rotate(angle + HALF_PI);\n            text(s.charAt(i), 0, 0);\n            rotate(-(angle + HALF_PI));\n            translate(-x, -y);\n        }\n    }\n    static createGraphics(s, circleSize, textFontSize, textColor, font) {\n        const radius = 0.5 * circleSize;\n        const graphicsSize = 2 * (radius + 2 * textFontSize);\n        // Unsolved issue: https://github.com/processing/p5.js/issues/2019\n\t\t\t  pixelDensity(1);\n        const graphics = createGraphics(graphicsSize, graphicsSize);\n\t\t\t  pixelDensity(displayDensity());\n        graphics.translate(0.5 * graphics.width, 0.5 * graphics.height);\n        textColor.applyToGraphics(graphics);\n        graphics.textAlign(CENTER);\n        if (font)\n            graphics.textFont(font, textFontSize);\n        else\n            graphics.textSize(textFontSize);\n        for (let i = 0, len = s.length; i < len; i += 1) {\n            const angle = (i / len) * TWO_PI - HALF_PI;\n            const x = radius * Math.cos(angle);\n            const y = radius * Math.sin(angle);\n            graphics.translate(x, y);\n            graphics.rotate(angle + HALF_PI);\n            graphics.text(s.charAt(i), 0, 0);\n            graphics.rotate(-(angle + HALF_PI));\n            graphics.translate(-x, -y);\n        }\n        return graphics;\n    }\n    draw() {\n        CircularText.draw(this.textString, this.circleSize);\n    }\n}\n//\n// ------------ Regular triangle shape -----------------------------\n//\n/// <reference path=\"../../../my_types/p5/p5.global-mode.d.ts\" />\n/// <reference path=\"Drawable.ts\" />\nclass RegularTriangleShape {\n    constructor(shapeSize) {\n        this.shapeSize = shapeSize;\n    }\n    static draw(shapeSize) {\n        const radius = 0.5 * shapeSize;\n        triangle(radius, 0, -RegularTriangleShape.COS60 * radius, -RegularTriangleShape.SIN60 * radius, -RegularTriangleShape.COS60 * radius, +RegularTriangleShape.SIN60 * radius);\n    }\n    static drawUpward(shapeSize) {\n        const radius = 0.5 * shapeSize;\n        triangle(0, radius, -RegularTriangleShape.SIN60 * radius, -RegularTriangleShape.COS60 * radius, +RegularTriangleShape.SIN60 * radius, -RegularTriangleShape.COS60 * radius);\n    }\n    // public static draw(shapeSize: number): void {\n    //   triangle(\n    //     RegularTriangleShape.TWO_THIRD * shapeSize,\n    //     0,\n    //     RegularTriangleShape.MINUS_ONE_THIRD * shapeSize,\n    //     -RegularTriangleShape.RECIPROCAL_SQRT_THREE * shapeSize,\n    //     RegularTriangleShape.MINUS_ONE_THIRD * shapeSize,\n    //     +RegularTriangleShape.RECIPROCAL_SQRT_THREE * shapeSize,\n    //   );\n    // }\n    // public static drawUpward(shapeSize: number): void {\n    //   triangle(\n    //     0,\n    //     RegularTriangleShape.MINUS_TWO_THIRD * shapeSize,\n    //     -RegularTriangleShape.RECIPROCAL_SQRT_THREE * shapeSize,\n    //     RegularTriangleShape.ONE_THIRD * shapeSize,\n    //     +RegularTriangleShape.RECIPROCAL_SQRT_THREE * shapeSize,\n    //     RegularTriangleShape.ONE_THIRD * shapeSize,\n    //   );\n    // }\n    draw() {\n        RegularTriangleShape.draw(this.shapeSize);\n    }\n}\n// private static readonly RECIPROCAL_SQRT_THREE = 1 / Math.sqrt(3);\n// private static readonly ONE_THIRD = 1 / 3;\n// private static readonly TWO_THIRD = 2 / 3;\n// private static readonly MINUS_ONE_THIRD = -1 / 3;\n// private static readonly MINUS_TWO_THIRD = -2 / 3;\nRegularTriangleShape.COS60 = 1 / 2;\nRegularTriangleShape.SIN60 = Math.sqrt(3) / 2;\n//\n// --------- ShapeColor (Composite of fill & stroke) -------------\n//\nclass AbstractShapeColor {\n    static createAlphaColorArray(c) {\n        const array = [];\n        for (let alphaValue = 0; alphaValue <= 255; alphaValue += 1) {\n            array.push(color(red(window.color(c)), green(window.color(c)), blue(window.color(c)), alpha(window.color(c)) * alphaValue / 255));\n        }\n        return array;\n    }\n}\nclass ShapeColor extends AbstractShapeColor {\n    constructor(strokeColor, fillColor) {\n        super();\n        this.strokeColorArray = AbstractShapeColor.createAlphaColorArray(strokeColor);\n        this.fillColorArray = AbstractShapeColor.createAlphaColorArray(fillColor);\n    }\n    apply(alphaValue = 255) {\n        const index = Math.floor(constrain(alphaValue, 0, 255));\n        stroke(this.strokeColorArray[index]);\n        fill(this.fillColorArray[index]);\n    }\n    applyToGraphics(graphics, alphaValue = 255) {\n        const index = Math.floor(constrain(alphaValue, 0, 255));\n        graphics.stroke(this.strokeColorArray[index]);\n        graphics.fill(this.fillColorArray[index]);\n    }\n}\nclass NoStrokeShapeColor extends AbstractShapeColor {\n    constructor(fillColor) {\n        super();\n        this.fillColorArray = AbstractShapeColor.createAlphaColorArray(fillColor);\n    }\n    apply(alphaValue = 255) {\n        noStroke();\n        const index = Math.floor(constrain(alphaValue, 0, 255));\n        fill(this.fillColorArray[index]);\n    }\n    applyToGraphics(graphics, alphaValue = 255) {\n        graphics.noStroke();\n        const index = Math.floor(constrain(alphaValue, 0, 255));\n        graphics.fill(this.fillColorArray[index]);\n    }\n}\nclass NoFillShapeColor extends AbstractShapeColor {\n    constructor(strokeColor) {\n        super();\n        this.strokeColorArray = AbstractShapeColor.createAlphaColorArray(strokeColor);\n    }\n    apply(alphaValue = 255) {\n        const index = Math.floor(constrain(alphaValue, 0, 255));\n        stroke(this.strokeColorArray[index]);\n        noFill();\n    }\n    applyToGraphics(graphics, alphaValue = 255) {\n        const index = Math.floor(constrain(alphaValue, 0, 255));\n        graphics.stroke(this.strokeColorArray[index]);\n        graphics.noFill();\n    }\n}\nclass NullShapeColor extends AbstractShapeColor {\n    apply() { }\n    applyToGraphics() { }\n}\n//\n// ------------ drawRotated() -----------------------------\n//\n/// <reference path=\"../../../../my_types/p5/p5.global-mode.d.ts\" />\n/// <reference path=\"../Drawable.ts\" />\nfunction drawRotated(drawableElement, position, rotationAngle) {\n    translate(position.x, position.y);\n    drawRotatedAroundOrigin(drawableElement, rotationAngle);\n    translate(-position.x, -position.y);\n}\nfunction drawRotatedAroundOrigin(drawableElement, rotationAngle) {\n    rotate(rotationAngle);\n    drawableElement.draw();\n    rotate(-rotationAngle);\n}\n//\n// ------------ Random string -----------------------------\n//\n/// <reference path=\"../../../../my_types/p5/p5.global-mode.d.ts\" />\nfunction randomString(charArray, strLen) {\n    let generatedString = '';\n    for (let i = 0; i < strLen; i += 1) {\n        generatedString += random(charArray);\n    }\n    return generatedString;\n}\n//\n// ------------ randomSign() -----------------------------\n//\nfunction randomSign(n) {\n    if (Math.random() < 0.5)\n        return n;\n    else\n        return -n;\n}\n//\n// ------------ Magic Circle -----------------------------\n//\n/// <reference path=\"../../my_types/p5/p5.global-mode.d.ts\" />\n/// <reference path=\"common/AngleQuantity.ts\" />\n/// <reference path=\"common/Sprite.ts\" />\n/// <reference path=\"common/CircularText.ts\" />\n/// <reference path=\"common/RegularTriangleShape.ts\" />\n/// <reference path=\"common/ShapeColor.ts\" />\n/// <reference path=\"common/functions/drawRotated.ts\" />\n/// <reference path=\"common/functions/randomString.ts\" />\n/// <reference path=\"common/functions/randomSign.ts\" />\nclass MagicCircle {\n    constructor() {\n        if (MagicCircle.isNotInitialized)\n            console.log('MagicCircle is not initialized.');\n        this.circularComponentArray = new SpriteArray();\n        this.triangleComponentArray = new SpriteArray();\n        for (let i = 0; i < 5; i += 1) {\n            this.addComponent('TRIANGLE');\n        }\n        for (let i = 0; i < 6; i += 1) {\n            this.addComponent(random(['CIRCLE', 'TEXT_CIRCLE', 'TEXT_CIRCLE']));\n        }\n    }\n    static initializeStatic(unitLength, unitAngleVelocity) {\n        this.unitLength = unitLength;\n        this.unitAngleVelocity = unitAngleVelocity;\n        this.isNotInitialized = false;\n    }\n    step() {\n        this.circularComponentArray.step();\n        this.triangleComponentArray.step();\n    }\n    draw() {\n        this.circularComponentArray.draw();\n        this.triangleComponentArray.draw();\n    }\n    addComponent(componentType) {\n        let shapeSize = 0;\n        switch (componentType) {\n            case 'CIRCLE':\n            case 'TEXT_CIRCLE':\n                if (this.circularComponentArray.length === 0) {\n                    shapeSize = random(30, 50) * MagicCircle.unitLength;\n                }\n                else {\n                    shapeSize\n                        = this.circularComponentArray.getLast().shapeSize\n                            + random(10, 60) * MagicCircle.unitLength;\n                }\n                break;\n            case 'TRIANGLE':\n                if (this.triangleComponentArray.length === 0) {\n                    shapeSize = random(40, 70) * MagicCircle.unitLength;\n                }\n                else {\n                    shapeSize\n                        = this.triangleComponentArray.getLast().shapeSize\n                            + random(60, 180) * MagicCircle.unitLength;\n                }\n                break;\n        }\n        switch (componentType) {\n            case 'TEXT_CIRCLE':\n                this.addTextCircle(shapeSize);\n                break;\n            case 'CIRCLE':\n                this.addEmptyCircle(shapeSize);\n                break;\n            case 'TRIANGLE':\n                this.addTriangle(shapeSize);\n                break;\n        }\n    }\n    addTextCircle(shapeSize) {\n        const textSize = random(10, 30) * unitLength;\n        const textCircle = new MagicTextCircle(createVector(0.5 * width, 0.5 * height), shapeSize + 4 * textSize, textSize, randomSign(random(0.005, 0.01)) * MagicCircle.unitAngleVelocity);\n        this.circularComponentArray.push(textCircle);\n    }\n    addEmptyCircle(shapeSize) {\n        const circle = new MagicEmptyCircle(createVector(0.5 * width, 0.5 * height), shapeSize, random(3, 8) * unitLength);\n        this.circularComponentArray.push(circle);\n    }\n    addTriangle(shapeSize) {\n        const triangle = new MagicTriangle(createVector(0.5 * width, 0.5 * height), shapeSize, random(1, 8) * unitLength, randomSign(random(0.02, 0.05)) * MagicCircle.unitAngleVelocity);\n        this.triangleComponentArray.push(triangle);\n    }\n}\nMagicCircle.isNotInitialized = true;\nclass MagicCircleElement {\n    constructor(shapeSize, inscribedCircleSize, drawableElement, position, angleVelocity) {\n        this.shapeSize = shapeSize;\n        this.inscribedCircleSize = inscribedCircleSize;\n        this.drawableElement = drawableElement;\n        this.position = position;\n        this.rotation = new AngleQuantity(random([0, PI]), angleVelocity);\n    }\n    step() {\n        this.rotation.step();\n    }\n    draw() {\n        drawRotated(this.drawableElement, this.position, this.rotation.angle);\n    }\n}\nclass LinedCircularText {\n    constructor(textString, circleSize, textSize) {\n        if (LinedCircularText.isNotInitialized)\n            console.log('LinedCircularText is not initialized.');\n        this.circleSize = circleSize;\n        this.inscribedCircleSize = circleSize - 3 * textSize;\n        this.circularTextGraphics = CircularText.createGraphics(textString, circleSize - 2 * textSize, textSize, LinedCircularText.textColor, LinedCircularText.font);\n    }\n    static initializeStatic(mainColor, font) {\n        this.textColor = new NoStrokeShapeColor(mainColor);\n        this.lineColor = new NoFillShapeColor(mainColor);\n        this.font = font;\n        this.isNotInitialized = false;\n    }\n    draw() {\n        LinedCircularText.lineColor.apply();\n        ellipse(0, 0, this.circleSize, this.circleSize);\n        ellipse(0, 0, this.inscribedCircleSize, this.inscribedCircleSize);\n        noStroke();\n        image(this.circularTextGraphics, 0, 0);\n    }\n}\nLinedCircularText.isNotInitialized = true;\nclass MagicEmptyCircle extends MagicCircleElement {\n    constructor(position, circleSize, lineWeight) {\n        const circle = { draw: () => { ellipse(0, 0, this.shapeSize, this.shapeSize); } };\n        super(circleSize, circleSize, circle, position, 0);\n        if (MagicEmptyCircle.isNotInitialized)\n            console.log('MagicEmptyCircle is not initialized.');\n        this.lineWeight = lineWeight;\n    }\n    static initializeStatic(mainColor) {\n        this.lineColor = new NoFillShapeColor(mainColor);\n        this.isNotInitialized = false;\n    }\n    draw() {\n        MagicEmptyCircle.lineColor.apply();\n        strokeWeight(this.lineWeight);\n        super.draw();\n    }\n}\nMagicEmptyCircle.isNotInitialized = true;\nclass MagicTextCircle extends MagicCircleElement {\n    constructor(position, circleSize, textSize, angleVelocity) {\n        const circularText = new LinedCircularText(randomString(MagicTextCircle.characterArray, 2 * Math.floor((circleSize) / (0.5 * textSize))), circleSize, textSize);\n        super(circleSize, circularText.inscribedCircleSize, circularText, position, angleVelocity);\n        if (MagicTextCircle.isNotInitialized)\n            console.log('MagicTextCircle is not initialized.');\n    }\n    static initializeStatic(unitLength) {\n        this.lineWeight = 1.5 * unitLength;\n        this.characterArray = [\n            'α', 'β', 'γ', 'δ', 'ε', 'ζ', 'η', 'θ', 'ι', 'κ', 'λ', 'μ',\n            'ν', 'ξ', 'ο', 'π', 'ρ', 'σ', 'τ', 'υ', 'φ', 'χ', 'ψ', 'ω',\n        ];\n        this.isNotInitialized = false;\n    }\n    draw() {\n        strokeWeight(MagicTextCircle.lineWeight);\n        super.draw();\n    }\n}\nMagicTextCircle.isNotInitialized = true;\nclass HexagramShape {\n    constructor(shapeSize) {\n        this.triangleShape = new RegularTriangleShape(shapeSize);\n    }\n    draw() {\n        this.triangleShape.draw();\n        rotate(PI);\n        this.triangleShape.draw();\n        rotate(-PI);\n    }\n}\nclass MagicTriangle extends MagicCircleElement {\n    constructor(position, shapeSize, lineWeight, angleVelocity) {\n        const triangle = new RegularTriangleShape(shapeSize);\n        super(shapeSize, 0.5 * shapeSize, triangle, position, angleVelocity);\n        if (MagicTriangle.isNotInitialized)\n            console.log('MagicTriangle is not initialized.');\n        this.lineWeight = lineWeight;\n    }\n    static initializeStatic(lineColor) {\n        this.lineColor = lineColor;\n        this.isNotInitialized = false;\n    }\n    draw() {\n        MagicTriangle.lineColor.apply();\n        strokeWeight(this.lineWeight);\n        super.draw();\n    }\n}\nMagicTriangle.isNotInitialized = true;\n/// <reference path=\"common/AlphaBackground.ts\" />\n/// <reference path=\"MagicCircle.ts\" />\n//\n// ------------ Global variables ------------------------------\n//\np5.disableFriendlyErrors = true;\nconst IDEAL_FRAME_RATE = 30;\nconst UNIT_ANGLE_VELOCITY = 2 * Math.PI / IDEAL_FRAME_RATE;\nlet unitLength;\nlet unitSpeed;\nconst fontPath = 'greek-subset.ttf';\nlet currentFont;\nlet alphaBackground;\nlet magicCircle;\n//\n// ------------ Setup & Draw etc. ---------------------------------------\n//\nfunction preload() {\n    currentFont = loadFont(fontPath);\n}\nfunction setup() {\n    const canvasSideLength = Math.max(windowWidth, windowHeight);\n    createCanvas(canvasSideLength, canvasSideLength);\n    frameRate(IDEAL_FRAME_RATE);\n    unitLength = Math.max(width, height) / 640;\n    unitSpeed = unitLength / IDEAL_FRAME_RATE;\n    strokeWeight(Math.max(1, 1 * unitLength));\n    MagicCircle.initializeStatic(unitLength, UNIT_ANGLE_VELOCITY);\n    LinedCircularText.initializeStatic(color(255), currentFont);\n    MagicTextCircle.initializeStatic(unitLength);\n    MagicEmptyCircle.initializeStatic(color(255));\n    MagicTriangle.initializeStatic(new NoFillShapeColor(color(255)));\n    textFont(currentFont);\n    textAlign(CENTER);\n    imageMode(CENTER);\n    alphaBackground = new AlphaBackground(color(16, 32, 32, 192), 1, DIFFERENCE, BLEND);\n    initialize();\n}\nfunction draw() {\n    alphaBackground.draw();\n    magicCircle.step();\n    magicCircle.draw();\n}\nfunction initialize() {\n    magicCircle = new MagicCircle();\n}\nfunction mousePressed() {\n    initialize();\n}\nfunction keyPressed() {\n    if (keyCode === 80)\n        noLoop(); // 80: 'P'\n}\nfunction keyReleased() {\n    if (keyCode === 80)\n        loop(); // 80: 'P'\n}\n\n// 1. 免疫 DOM 元素建立與樣式操作導致的看門狗攔截\n// 建立一個既能當函數呼叫 (如 style())，又是具有各屬性的 Proxy 物件，防止任何 .style、.style()、.position 錯誤\n(function() {\n  const handler = {\n    get: function(target, prop) {\n      if (prop === 'style') {\n        const styleFunc = function() { return styleProxy; };\n        Object.setPrototypeOf(styleFunc, styleProxy);\n        return styleFunc;\n      }\n      if (typeof target[prop] === 'function') {\n        return target[prop].bind(target);\n      }\n      return styleProxy;\n    }\n  };\n  const dummyObject = function() {};\n  dummyObject.position = function() { return dummyObject; };\n  dummyObject.style = function() { return dummyObject; };\n  dummyObject.size = function() { return dummyObject; };\n  dummyObject.parent = function() { return dummyObject; };\n  dummyObject.id = function() { return \"\"; };\n  dummyObject.class = function() { return dummyObject; };\n  dummyObject.mousePressed = function() { return dummyObject; };\n  dummyObject.mouseOver = function() { return dummyObject; };\n  dummyObject.changed = function() { return dummyObject; };\n  dummyObject.html = function() { return dummyObject; };\n  dummyObject.value = function() { return 0; };\n  \n  const styleProxy = new Proxy(dummyObject, handler);\n  \n  if (typeof createP === 'undefined') { window.createP = function() { return styleProxy; }; }\n  if (typeof createDiv === 'undefined') { window.createDiv = function() { return styleProxy; }; }\n  if (typeof createButton === 'undefined') { window.createButton = function() { return styleProxy; }; }\n  if (typeof createSlider === 'undefined') { window.createSlider = function() { return styleProxy; }; }\n  if (typeof select === 'undefined') { window.select = function() { return styleProxy; }; }\n  if (typeof selectAll === 'undefined') { window.selectAll = function() { return [styleProxy]; }; }\n})();\n\n// 1.2 免疫 HTML 屬性與 Element.prototype.style 崩潰 (確保 style 與 checked 既可以當物件又可以當函數呼叫)\ntry {\n  if (typeof Element !== 'undefined') {\n    const dummyObj = function() { return dummyObj; };\n    const p = new Proxy(dummyObj, {\n      get: function(target, prop) {\n        if (typeof target[prop] === 'function') return target[prop];\n        return p;\n      }\n    });\n    \n    // 用屬性定義防止被覆寫\n    Object.defineProperty(Element.prototype, 'style', {\n      get: function() {\n        const styleFunc = function() { return p; };\n        Object.setPrototypeOf(styleFunc, p);\n        return styleFunc;\n      },\n      set: function() {},\n      configurable: true\n    });\n    \n    Object.defineProperty(Element.prototype, 'checked', {\n      get: function() {\n        const checkedFunc = function() { return false; };\n        return checkedFunc;\n      },\n      set: function() {},\n      configurable: true\n    });\n  }\n} catch(e) {}\n\n// 1.3 免疫 HTMLInputElement.size = 0 或非法負數導致的瀏覽器拋錯\ntry {\n  if (typeof HTMLInputElement !== 'undefined') {\n    const desc = Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'size');\n    if (desc && desc.set) {\n      const originalSet = desc.set;\n      Object.defineProperty(HTMLInputElement.prototype, 'size', {\n        set: function(val) {\n          if (val <= 0) val = 20; // 設為預設合規大小\n          originalSet.call(this, val);\n        },\n        configurable: true\n      });\n    }\n  }\n} catch(e) {}\n\n// 1.5 免疫 PVector 與 beginGeometry 缺失導致的崩潰\nif (typeof PVector === 'undefined') {\n    window.PVector = class PVector {\n        constructor(x, y, z) {\n            this.x = x || 0;\n            this.y = y || 0;\n            this.z = z || 0;\n        }\n        static dist(v1, v2) {\n            return Math.sqrt((v1.x - v2.x) ** 2 + (v1.y - v2.y) ** 2 + (v1.z - v2.z) ** 2);\n        }\n        set(x, y, z) {\n            this.x = x || 0;\n            this.y = y || 0;\n            this.z = z || 0;\n        }\n    };\n}\nif (typeof beginGeometry === 'undefined') { window.beginGeometry = function() {}; }\nif (typeof endGeometry === 'undefined') { window.endGeometry = function() { return { draw: function() {} }; }; }\n\n// 1.6 修正舊版 Processing/Canvas 內建函數缺失 (如 createFont, concat, append, _renderer 缺失)\nif (typeof createFont === 'undefined') { window.createFont = function(name, size) { return name; }; }\nif (typeof concat === 'undefined') { window.concat = function(a, b) { return (a || []).concat(b || []); }; }\nif (typeof append === 'undefined') { window.append = function(array, value) { if(array) { array.push(value); } return array; }; }\nif (typeof _renderer === 'undefined') { window._renderer = { _setAttributes: function() {} }; }\n\n// 1.7 免疫機器學習與人臉偵測 (ml5.js, clmtrackr, aiptag, lime) 缺失與內部指標 null 導致的崩潰 (修復 ml5.js .features of null 報錯)\nif (typeof window.clm === 'undefined') {\n    window.clm = {\n        tracker: class {\n            init() {}\n            start() {}\n            track() { return []; }\n            getCurrentPosition() { return []; }\n            getScore() { return 0; }\n        }\n    };\n}\nif (typeof window.ml5 === 'undefined') {\n    const ml5MockObj = {\n        detectStart: function() {},\n        features: { get: function() { return []; } },\n        on: function() {},\n        ready: Promise.resolve(),\n        loadModel: Promise.resolve()\n    };\n    window.ml5 = {\n        bodypix: function() { return ml5MockObj; },\n        faceapi: function() { return ml5MockObj; },\n        handpose: function() { return ml5MockObj; },\n        poseNet: function() { return ml5MockObj; },\n        objectDetector: function() { return ml5MockObj; }\n    };\n}\nif (typeof window.aiptag === 'undefined') { window.aiptag = {}; }\nif (typeof window.lime === 'undefined') { window.lime = {}; }\n\n// 1.8 免疫 3D 物理引擎與高階渲染 (THREE.Group, Tone.js, p5.asciify, REGL, Voronoi, PolyK, qrcode, P5Capture) 缺失導致的崩潰\nif (typeof window.THREE === 'undefined') {\n    window.THREE = {\n        Group: class {},\n        Scene: class {},\n        PerspectiveCamera: class {},\n        WebGLRenderer: class { setSize() {} },\n        Mesh: class {},\n        BoxGeometry: class {}\n    };\n}\nif (typeof window.Tone === 'undefined') {\n    window.Tone = {\n        Player: class { play() {} start() {} stop() {} connect() {} toDestination() {} },\n        Players: class { play() {} start() {} stop() {} connect() {} toDestination() {} },\n        Sampler: class { triggerAttackRelease() {} connect() {} toDestination() {} },\n        Sequence: class { start() {} stop() {} },\n        Transport: { start: function() {}, stop: function() {}, bpm: { value: 120 } }\n    };\n}\nif (typeof window.createREGL === 'undefined') {\n    window.createREGL = function() {\n        const regl = function() {};\n        regl.texture = function() { return { resize: function() {} }; };\n        regl.buffer = function() { return { subdata: function() {} }; };\n        regl.framebuffer = function() { return { use: function(cb) { cb(); } }; };\n        return regl;\n    };\n}\nif (typeof window.Voronoi === 'undefined') {\n    window.Voronoi = class {\n        compute() { return { cells: [], edges: [] }; }\n    };\n}\nif (typeof window.qrcode === 'undefined') {\n    window.qrcode = function(data) {\n        return {\n            createImgTag: function() { return \"\"; },\n            createTableTag: function() { return \"\"; },\n            createSvgTag: function() { return \"\"; }\n        };\n    };\n    window.qrcode.stringToBytes = function(s) { return []; };\n}\nif (typeof window.PolyK === 'undefined') {\n    window.PolyK = {\n        Slice: function() { return []; },\n        Triangulate: function() { return []; },\n        getArea: function() { return 0; }\n    };\n}\nif (typeof window.P5Capture === 'undefined') {\n    window.P5Capture = {\n        getInstance: function() {\n            return { start: function() {}, stop: function() {}, setFormat: function() {} };\n        },\n        setDefaultOptions: function() {}\n    };\n}\n\n// 1.9 免疫 OPC (OpenProcessing Controller) 與 WebGL 內部 setUniform 崩潰\nif (typeof window.OPC === 'undefined') {\n    window.OPC = {\n        title: function() {},\n        slider: function() {},\n        button: function() {},\n        toggle: function() {},\n        color: function() {}\n    };\n}\nif (typeof p5 !== 'undefined' && p5.Shader) {\n    const origSetUniform = p5.Shader.prototype.setUniform;\n    p5.Shader.prototype.setUniform = function(name, val) {\n        if (!this) return this;\n        try {\n            return origSetUniform.call(this, name, val);\n        } catch(e) {\n            return this;\n        }\n    };\n}\n\n// 1.95 免疫某些特定遊戲模組對 parent / maeExportApis_ 或是唯讀屬性的寫入崩潰\ntry {\n  if (typeof window.parent !== 'undefined') {\n    if (!window.parent.maeExportApis_) {\n      try {\n        window.parent.maeExportApis_ = function() {};\n      } catch(e) {}\n    }\n  }\n} catch(e) {}\n\n// 1.96 免疫 textmode 缺失以及 loadSVG / p5.SVG 插件型別拋錯\nif (typeof textmode === 'undefined') { window.textmode = function() {}; }\nif (typeof MAT === 'undefined') { window.MAT = []; }\nif (typeof SIDES === 'undefined') { window.SIDES = 4; }\nif (typeof GRIDBOX === 'undefined') { window.GRIDBOX = 10; }\nif (typeof COLUMNS === 'undefined') { window.COLUMNS = 5; }\nif (typeof tileSize === 'undefined') { window.tileSize = 30; }\nif (typeof mouseTileX === 'undefined') { window.mouseTileX = 0; }\nif (typeof p5 !== 'undefined') {\n  if (!p5.prototype.loadSVG) {\n    p5.prototype.loadSVG = function(path, success, failure) {\n      if (success) success({});\n      return {};\n    };\n  }\n  // 免疫 window.VERSION 唯讀寫入錯誤\n  try {\n    Object.defineProperty(window, 'VERSION', {\n      value: \"p5-stub\",\n      writable: true,\n      configurable: true\n    });\n  } catch(e) {}\n}\n\n// 2. 圖片與非同步資產載入後備自癒護欄\nif (typeof p5 !== 'undefined' && p5.prototype) {\n    if (typeof window._origLoadImage === 'undefined') { window._origLoadImage = p5.prototype.loadImage; }\n    p5.prototype.loadImage = function(path, successCallback, failureCallback) {\n        if (typeof path !== 'string' || path.startsWith('http') || (path.startsWith('data:') === false && path.indexOf('.') === -1)) {\n            // 當發現是遠端 URL、相對路徑或格式無副檔名時，使用 1x1 灰色 GIF 的 Base64 代替，防止渲染死鎖與 CORS 錯誤\n            const dummyPath = \"data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7\";\n            return window._origLoadImage.call(this, dummyPath, successCallback, failureCallback);\n        }\n        return window._origLoadImage.call(this, path, successCallback, failureCallback);\n    };\n    \n    // 修正 img.loadPixels / img.resize 缺失與未非同步載入完成呼叫\n    if (!p5.Image.prototype.loadPixels) { p5.Image.prototype.loadPixels = function() { return this; }; }\n    if (!p5.Image.prototype.resize) { p5.Image.prototype.resize = function() { return this; }; }\n    if (!p5.Image.prototype.get) { p5.Image.prototype.get = function() { return [0,0,0,255]; }; }\n    if (!p5.Image.prototype.width) {\n        Object.defineProperty(p5.Image.prototype, 'width', {\n            get: function() { return this._width || 1280; },\n            set: function(v) { this._width = v; }\n        });\n    }\n    if (!p5.Image.prototype.height) {\n        Object.defineProperty(p5.Image.prototype, 'height', {\n            get: function() { return this._height || 720; },\n            set: function(v) { this._height = v; }\n        });\n    }\n    \n    // 修正 p5.Graphics / p5.Renderer / Element.prototype.style\n    if (p5.Graphics && p5.Graphics.prototype) {\n        if (!p5.Graphics.prototype.loadPixels) { p5.Graphics.prototype.loadPixels = function() { return this; }; }\n    }\n}\n\n// 2.2 修正 p5.Table / loadTable / getRowCount 缺失與錯誤\nif (typeof p5 !== 'undefined' && p5.Table) {\n    if (!p5.Table.prototype.getRowCount) { p5.Table.prototype.getRowCount = function() { return 0; }; }\n}\n\n// 3. 修正 3D 渲染圖層 WebGL 與 Canvas 2D 上下文屬性缺失\nif (typeof p5 !== 'undefined' && p5.prototype) {\n    if (typeof window._origGet === 'undefined') { window._origGet = p5.prototype.get; }\n    p5.prototype.get = function(...args) {\n        if (!this || this.width === 0 || this.height === 0) {\n            return [0,0,0,255];\n        }\n        try {\n            return window._origGet.apply(this, args);\n        } catch(e) {\n            return [0,0,0,255];\n        }\n    };\n}\n",
    "custom_html": "<script src=\"https://cdn.jsdelivr.net/npm/csslint@1.0.5/dist/csslint.js\"></script>\n",
    "custom_css": "/* Auto-hidden controls */\ninput, select, button, textarea, label, fieldset,\n.dg, .lil-gui, .qs_main, .opc-container, .opc-gui, .quicksettings, .p5DomElement {\n    display: none !important;\n    visibility: hidden !important;\n    opacity: 0 !important;\n    pointer-events: none !important;\n    position: absolute !important;\n    left: -9999px !important;\n    top: -9999px !important;\n    width: 0px !important;\n    height: 0px !important;\n}",
    "author": "",
    "license": "",
    "scaling_mode": "auto"
}