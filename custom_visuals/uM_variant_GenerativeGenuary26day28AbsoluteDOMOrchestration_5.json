{
    "name": "Uncler M - GenerativeGenuary26day28AbsoluteDOMOrchestration_var5",
    "code": "\n// --- Safety Guardrails for Thumbnail and Sandbox ---\nif (typeof width === 'undefined') { var width = 1280; }\nif (typeof height === 'undefined') { var height = 720; }\n\n\n// --- Global Variables for Uncler M Variants ---\nvar uM_colors = [\"#1e3a8a\", \"#d97706\", \"#f59e0b\", \"#3b82f6\", \"#172554\"];\nvar speedMult = 1.0;\nvar lineScale = 1.0;\nvar dynamicShift = 0;\nvar scalePulse = 1.0;\nvar rotAngle = 0;\nvar gridOffset = 0;\nvar uM_rot = 0;\n\n// === Inspired by 'GenerativeGenuary26day28AbsoluteDOMOrchestration' by 'Kazoops' ===\n// Created as variant #5 by Uncler M\n// Infused with creative algorithmic modifications.\n\n// === Tab: mySketch.js ===\n// --- OpenProcessing Platform Bug Fix ---\nif (!window.OPRuler) { // Check if the OPRuler object is missing\n    window.OPRuler = { hide: function() {} }; // Create a dummy hide function to prevent platform errors\n} // End of bug fix block\n\n// --- Global Variables ---\nvar ringData = []; // Initialize an array to store ring objects and their properties\nvar totalRings = 60; // Define the total number of ring elements to be created\nvar fov = 500; // Define the field of view constant for 3D projection calculations\nvar stage; // Declare a variable to hold the main container element\n\nfunction setup() { // Standard initialization function called once by the environment\n    if (typeof noCanvas === 'function') noCanvas(); // Tell the environment not to create or expect a canvas\n\n    var css = document.createElement('style'); // Create a new style element for custom CSS rules\n    css.innerHTML = // Define the CSS rules for the body, stage, and rings\n        'body { margin: 0; background: #000; overflow: hidden; height: 100vh; width: 100vw; } ' + // Setup body styles\n        '#container { position: absolute; top: 50%; left: 50%; width: 0; height: 0; } ' + // Center the stage\n        '.ring-div { position: absolute; border: 2.5px solid; border-radius: 50%; box-sizing: border-box; pointer-events: none; }'; // Style the rings\n    document.head.appendChild(css); // Append the style rules to the document head\n\n    stage = document.createElement('div'); // Create a new div element to serve as the container\n    stage.id = 'container'; // Set the ID of the container for reference\n    document.body.appendChild(stage); // Append the container to the document body\n\n    for (var i = 0; i < totalRings; i++) { // Loop to generate the specified number of rings\n        var el = document.createElement('div'); // Create a new div for each individual ring\n        el.className = 'ring-div'; // Assign the ring class for styling\n        var baseSize = 300; // Define the base diameter of the rings in pixels\n        el.style.width = baseSize + 'px'; // Set the width of the ring element\n        el.style.height = baseSize + 'px'; // Set the height of the ring element\n        el.style.left = -(baseSize / 2) + 'px'; // Center the ring horizontally\n        el.style.top = -(baseSize / 2) + 'px'; // Center the ring vertically\n        stage.appendChild(el); // Add the ring element to the stage container\n        \n        ringData.push({ // Store the ring element and its unique properties in the array\n            el: el, // Reference to the DOM element\n            offset: i * 0.12, // Staggered timing offset for each ring\n            hueBase: i * 6 // Starting hue based on the ring index\n        }); // End of object push\n    } // End of ring generation loop\n} // End of setup function\n\nfunction draw() {\n  // === Variant 5: Symmetric Kaleidoscope Portal ===\n  // Applying kaleidoscope coordinate distortion\n  uM_rot = frameCount * 0.005;\n  push();\n  translate(width/2, height/2);\n  rotate(uM_rot);\n  scale(1.1);\n  translate(-width/2, -height/2);\n // Animation loop function called 60 times per second\n    if (!stage) return; // Exit if the stage has not been initialized yet\n    var t = frameCount * 0.02; // Calculate a time variable based on the frame count\n\n    for (var i = 0; i < ringData.length; i++) { // Iterate through each ring in the data array\n        var r = ringData[i]; // Get a reference to the current ring object\n        var z = Math.sin(t + r.offset) * 350; // Calculate the Z-depth using a sine wave\n        var x3d = Math.cos(t * 0.7 + r.offset) * 150; // Calculate horizontal oscillation\n        var y3d = Math.sin(t * 0.5 + r.offset) * 100; // Calculate vertical oscillation\n\n        var scl = fov / (fov + z); // Convert 3D Z-depth into a 2D scale factor\n        var tx = x3d * scl; // Project the 3D X coordinate to 2D space\n        var ty = y3d * scl; // Project the 3D Y coordinate to 2D space\n\n        var h = (r.hueBase + frameCount * 1.5) % 360; // Calculate the shifting hue for the rainbow effect\n        var opac = (z + 400) / 800; // Adjust opacity based on depth for a fading effect\n\n        r.el.style.transform = 'translate(' + tx + 'px,' + ty + 'px) scale(' + scl + ')'; // Apply 2D position and scale\n        r.el.style.borderColor = 'hsl(' + h + ', 85%, 60%)'; // Set the ring's color using HSL values\n        r.el.style.boxShadow = '0 0 ' + (15 * scl) + 'px hsl(' + h + ', 80%, 40%)'; // Apply a glow that scales with size\n        r.el.style.opacity = opac; // Set the calculated opacity to the element\n        r.el.style.zIndex = Math.floor(1000 - z); // Update the stacking order based on depth\n    } // End of ring calculation loop\n\n  pop();\n} // End of draw function\n\n// 1. 免疫 DOM 元素建立導致的看門狗攔截\nif (typeof createP === 'undefined') { window.createP = function() { return { position: function(){}, style: function(){} }; }; }\nif (typeof createDiv === 'undefined') { window.createDiv = function() { return { position: function(){}, style: function(){} }; }; }\n\n// 2. 圖片與非同步資產載入後備自癒護欄\nif (typeof p5 !== 'undefined' && p5.prototype) {\n    if (typeof window._origLoadImage === 'undefined') { window._origLoadImage = p5.prototype.loadImage; }\n    p5.prototype.loadImage = function(path, successCallback, failureCallback) {\n        if (typeof path !== 'string' || (path.startsWith('http') === false && path.startsWith('data:') === false)) {\n            // 當發現是相對路徑或丟失的外部圖片資產時，使用 1x1 灰色 GIF 的 Base64 代替，防止渲染死鎖\n            const dummyPath = \"data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7\";\n            return window._origLoadImage.call(this, dummyPath, successCallback, failureCallback);\n        }\n        return window._origLoadImage.call(this, path, successCallback, failureCallback);\n    };\n}\n\n// 3. 修正 3D 渲染圖層 WebGL 與 Canvas 2D 上下文屬性缺失\nif (typeof p5 !== 'undefined' && p5.prototype) {\n    if (typeof window._origGet === 'undefined') { window._origGet = p5.prototype.get; }\n    p5.prototype.get = function(...args) {\n        if (this.width === 0 || this.height === 0) {\n            return createGraphics(10, 10);\n        }\n        return window._origGet.apply(this, args);\n    };\n}\n",
    "frequency": 50,
    "storyboard_weight": 50,
    "post_fx_intensity": 50,
    "custom_html": "<script src=\"https://cdn.jsdelivr.net/npm/csslint@1.0.5/dist/csslint.js\"></script>\n   \n\t\n\n",
    "custom_css": "html,\nbody {\n\tmargin: 0;\n\tpadding: 0;\n}\n",
    "inline_assets": {},
    "author": "Uncler M",
    "license": "CC BY-NC-SA",
    "tags": [
        "Uncler M",
        "generative",
        "variant",
        "starred_GenerativeGenuary26day28AbsoluteDOMOrchestration"
    ],
    "url": "https://openprocessing.org/@Kazoops/2845382",
    "date_added": "2026-07-08 23:13:00",
    "is_starred": false
}