{
  "name": "Ondas2",
  "code": "// === Tab: mySketch ===\nvar colors = [\"#3ec1d3\", \"#f6f7d7\", \"#f6f7d7\", \"#ff165d\"];\nvar mySeed;\n\nfunction setup() {\n  createCanvas(1000, 1000);\n  rectMode(CENTER);\n  ellipseMode(CENTER);\n  strokeCap(SQUARE);\n  colorMode(HSB, 360, 100, 100, 100);\n  mySeed = floor(random(1, 100000));\n}\n\nfunction draw() {\n  randomSeed(mySeed);\n\n fill(0);\n square(width/2,height/2,width);\n\n  let mySize = 100;\n\t\n  for (let xx = mySize; xx <= width - mySize; xx += mySize) {\n    for (let yy = mySize; yy <= height - mySize; yy += mySize) {\n\n      let xPos  = xx;\n      let yPos  = yy;\n      let amp   = mySize / 4;\n      let ang   = random([0, PI/2, PI, 3*PI/2]);\n      let step  = random(1, 6);\n      let len   = mySize*0.75;\n      let thick = 2;\n      let freq  = random(1,3);\n\t\t  \n      drawSquareWave(xPos, yPos, freq, amp, ang, step, len, thick);\n\t\tdrawWave(xPos, yPos, freq, amp, ang, step, len, thick);\n\t\t\n    }\n  }\n}\n\nfunction drawWave(xpos, ypos, freq, amp, ang, step, len, thick) {\n  push();\n  translate(xpos, ypos);\n  rotate(ang);\n  const strokeCol = generateColor();\n  const fillCol   = generateColor();\n  strokeWeight(thick);\n  stroke(strokeCol);\n  fill(fillCol);\n\tnoFill();\n  const phase = 0.025*frameCount;\n\n  beginShape();\n  for (let xx = -len / 2; xx <= len / 2; xx += step) {\n    amp -= 1;\n    const yy = amp * cos(xx*freq + phase);\n    curveVertex(xx, yy);\n  }\n\n  endShape();\n  pop();\n}\nfunction drawSquareWave(xpos, ypos, freq, amp, ang, step, len, thick) {\n  push();\n  translate(xpos, ypos);\n  rotate(ang);\n\n  var strokeCol = generateColor();\n  var fillCol   = generateColor();\n  strokeWeight(thick);\n  stroke(strokeCol);\n  noFill();  \n\n  var phase = 0.025 * frameCount;\n\n  beginShape();\n  let prevY = null;\n\n  for (let xx = -len / 2; xx <= len / 2; xx += step) {\n    const s  = cos(xx * freq + phase);\n    var yy = (s >= 0) ? amp : -amp;\n\n    if (prevY === null) {\n      vertex(xx, yy);\n    } else {\n      if (yy !== prevY) {\n        vertex(xx, prevY);\n        vertex(xx, yy);\n      } else {\n        vertex(xx, yy);\n      }\n    }\n    prevY = yy;\n  }\n\n  endShape();\n  pop();\n}\n\n\nfunction generateColor() {\n  let myColor = color(random(colors));\n  myColor.setAlpha(random(50, 100));\n  return myColor;\n}",
  "frequency": 50,
  "storyboard_weight": 50,
  "post_fx_intensity": 50,
  "custom_html": "<script src=\"https://cdn.jsdelivr.net/npm/csslint@1.0.5/dist/csslint.js\"></script>\n",
  "custom_css": "",
  "inline_assets": {},
  "author": "garabatospr",
  "license": "CC BY-NC-SA",
  "tags": [
    "batch_import",
    "audio_reactive"
  ],
  "url": "https://openprocessing.org/@garabatospr/2807867",
  "date_added": "2026-07-06 21:04:58",
  "is_starred": false,
  "used_count": 8,
  "scaling_mode": "cover",
  "used_in_videos": [
    "Clear Sky.mp4",
    "Cold Vacuum.mp4",
    "Concrete Tides.mp4",
    "Icebound Groove.mp4",
    "Monolithic Interval.mp4",
    "Vacuum Memory.mp4",
    "Ultimate Core Patterns.mp4"
  ]
}