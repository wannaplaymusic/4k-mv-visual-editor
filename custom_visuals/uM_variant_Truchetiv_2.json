{
    "name": "Uncler M - Truchetiv_var2",
    "code": "\n// --- Safety Guardrails for Thumbnail and Sandbox ---\nif (typeof width === 'undefined') { var width = 1280; }\nif (typeof height === 'undefined') { var height = 720; }\n\n\n// --- Global Variables for Uncler M Variants ---\nvar uM_colors = [\"#f97316\", \"#ef4444\", \"#ec4899\", \"#8b5cf6\", \"#facc15\"];\nvar speedMult = 1.0;\nvar lineScale = 1.0;\nvar dynamicShift = 0;\nvar scalePulse = 1.0;\nvar rotAngle = 0;\nvar gridOffset = 0;\nvar uM_rot = 0;\n\n// === Inspired by 'Truchetiv' by 'Rubén Medellín &lt;chubas&gt;' ===\n// Created as variant #2 by Uncler M\n// Infused with creative algorithmic modifications.\n\n// === Tab: mySketch ===\nlet tileCols;\nlet cw, tileSize, cols, rows;\nlet gridOffsetX, gridOffsetY, tileGrid;\nlet colorA = 255, colorB = 0;\n\nfunction setup() {\n  cw = max(windowWidth, windowHeight);\n  createCanvas(cw, cw);\n  randomSeed(floor(random(1e9)));\n  tileCols = floor(random(10, 30));\n  tileSize = max(24, floor(width / tileCols));\n  cols = floor(width / tileSize);\n  rows = floor(height / tileSize);\n\n  gridOffsetX = round((width - cols * tileSize) / 2);\n  gridOffsetY = round((height - rows * tileSize) / 2);\n\n  tileGrid = [];\n  for (let row = 0; row < rows; row++) {\n    tileGrid[row] = [];\n    for (let col = 0; col < cols; col++) {\n      const even = (col + row) % 2 === 0;\n      tileGrid[row][col] = even ? random([0, 3]) : random([1, 2]);\n    }\n  }\n  noStroke();\n}\n\nfunction draw() {\n  // === Variant 2: Organic Lissajous Morph ===\n  scalePulse = 1.0 + 0.25 * sin(frameCount * 0.03);\n  rotAngle = frameCount * 0.015;\n  push();\n  translate(width/2, height/2);\n  rotate(rotAngle);\n  scale(scalePulse);\n  translate(-width/2, -height/2);\n\n  background(colorA);\n  for (let row = 0; row < rows; row++) {\n    for (let col = 0; col < cols; col++) {\n      const x = gridOffsetX + col * tileSize;\n      const y = gridOffsetY + row * tileSize;\n      drawTruchetTile(tileGrid[row][col], x, y, tileSize);\n    }\n  }\n\n  pop();\n}\n\nfunction drawTruchetTile(variant, x, y, size) {\n  push();\n  let distanceToMouse = dist(x, y, (window.simulatedMouseX !== undefined ? window.simulatedMouseX : (typeof sub_bass !== 'undefined' ? map(sub_bass, 0, 1, width*0.1, width*0.9) : (typeof live_centroid !== 'undefined' ? map(live_centroid, 100, 4000, 0, width) : mouseX))), (window.simulatedMouseY !== undefined ? window.simulatedMouseY : (typeof percussive !== 'undefined' ? map(percussive, 0, 1, height, 0) : (typeof roughness !== 'undefined' ? map(roughness, 0, 1, height*0.2, height*0.8) : mouseY))));\n  let roundness = map(distanceToMouse, size, max(10 * size, cw / 3), size/2, 0, true);\n  translate(x, y);\n  clip(() => rect(0, 0, size, size));\n\n  rectMode(CENTER);\n  noStroke();\n  let fillA = variant === 0 || variant === 1 ? colorA : colorB;\n  let fillB = variant === 0 || variant === 1 ? colorB : colorA;\n  fill(fillA);\n  rect(size/2, size/2, size, size);\n  fill(fillB);\n  if (variant === 0 || variant === 2) {\n    rect(0, 0, size, size, roundness);\n    rect(size, size, size, size, roundness);\n  } else {\n    rect(size, 0, size, size, roundness);\n    rect(0, size, size, size, roundness);\n  }\n  pop();\n}\n\n\n// 1. 免疫 DOM 元素建立導致的看門狗攔截\nif (typeof createP === 'undefined') { window.createP = function() { return { position: function(){}, style: function(){} }; }; }\nif (typeof createDiv === 'undefined') { window.createDiv = function() { return { position: function(){}, style: function(){} }; }; }\n\n// 2. 圖片與非同步資產載入後備自癒護欄\nif (typeof p5 !== 'undefined' && p5.prototype) {\n    if (typeof window._origLoadImage === 'undefined') { window._origLoadImage = p5.prototype.loadImage; }\n    p5.prototype.loadImage = function(path, successCallback, failureCallback) {\n        if (typeof path !== 'string' || (path.startsWith('http') === false && path.startsWith('data:') === false)) {\n            // 當發現是相對路徑或丟失的外部圖片資產時，使用 1x1 灰色 GIF 的 Base64 代替，防止渲染死鎖\n            const dummyPath = \"data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7\";\n            return window._origLoadImage.call(this, dummyPath, successCallback, failureCallback);\n        }\n        return window._origLoadImage.call(this, path, successCallback, failureCallback);\n    };\n}\n\n// 3. 修正 3D 渲染圖層 WebGL 與 Canvas 2D 上下文屬性缺失\nif (typeof p5 !== 'undefined' && p5.prototype) {\n    if (typeof window._origGet === 'undefined') { window._origGet = p5.prototype.get; }\n    p5.prototype.get = function(...args) {\n        if (this.width === 0 || this.height === 0) {\n            try {\n                if (typeof this.createGraphics === 'function') {\n                    return this.createGraphics(10, 10);\n                } else if (typeof createGraphics === 'function') {\n                    return createGraphics(10, 10);\n                }\n            } catch(e) {}\n            try {\n                if (typeof this.createImage === 'function') {\n                    return this.createImage(1, 1);\n                }\n            } catch(e) {}\n            return {\n                width: 1,\n                height: 1,\n                pixels: [0, 0, 0, 0],\n                loadPixels: function() {},\n                updatePixels: function() {},\n                get: function() { return [0, 0, 0, 255]; },\n                vertices: []\n            };\n        }\n        try {\n            return window._origGet.apply(this, args);\n        } catch(e) {\n            return {\n                width: 1,\n                height: 1,\n                pixels: [0, 0, 0, 0],\n                loadPixels: function() {},\n                updatePixels: function() {},\n                get: function() { return [0, 0, 0, 255]; },\n                vertices: []\n            };\n        }\n    };\n}\n",
    "frequency": 50,
    "storyboard_weight": 50,
    "post_fx_intensity": 50,
    "custom_html": "<script src=\"https://cdn.jsdelivr.net/npm/csslint@1.0.5/dist/csslint.js\"></script>\n",
    "custom_css": "",
    "inline_assets": {},
    "author": "Uncler M",
    "license": "CC BY-NC-SA",
    "tags": [
        "Uncler M",
        "generative",
        "variant",
        "starred_Truchetiv"
    ],
    "url": "https://openprocessing.org/@chubas/2915133",
    "date_added": "2026-07-08 23:13:00",
    "is_starred": false,
    "used_in_videos": [
        "Acid Rain Car Chase.mp4"
    ],
    "used_count": 1
}